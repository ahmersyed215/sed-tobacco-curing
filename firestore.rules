rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Internal admin app: any signed-in user can read/write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
